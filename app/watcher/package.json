{
  "name": "daw-watcher",
  "version": "0.1.2",
  "description": "DAW file watcher and uploader with automatic encryption",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "cd src-frontend && npm run build && cd ..",
    "build:dist": "npm run build && node build-dist.js",
    "test": "cargo test"
  },
  "devDependencies": {
    "@tauri-apps/api": "^1.5.0",
    "@tauri-apps/cli": "^1.6.3"
  }
}
